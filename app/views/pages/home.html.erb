<div class="background" id="logo" style= "display: block">
<img src="<%=image_path "Logo_sound_On.png"%>">
</div>
  <div class="background-home" id="background" style= "display: none">
    <div class="container justify-content-center">
      <div class="soundon" id="soundon">
      <% if user_signed_in? %>

        <h5 style="padding-top: 21px; text-align: left;">Hello Célia ! </h5>
      <% else %>
      <div class="space">
        <h5 style="padding-top: 21px; text-align: left; font-size: 26px">Welcome to SoundOn ! </h5>
      </div>
      <% end %>
        <h6 style="padding-bottom: 20px; text-align: left; font-size: 26px">Guess to stay on track ! 🚀</h6>
        <div class="sound-on-center">
          <h2 style="padding-top: 97px; font-weight: bold; font-size: 26px">Let's play a game</h2>
            <div class="start-button" id="start-button" >
            <%= link_to (user_signed_in? ? new_game_path : user_spotify_omniauth_authorize_path) do %>
              <img src="<%=image_path "button_purple.png"%>" style="padding-bottom: 32px">
            <% end %>
            </div>
        </div>
         <div class="d-flex justify-content-center" id="spotify">
          <%= link_to user_signed_in? ? "Login with another account" : "Login", user_spotify_omniauth_authorize_path, class: "btn btn-spotify" %>
         </div>
      </div>
    </div>
  </div>
