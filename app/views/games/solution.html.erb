
<div class="d-flex flex-column align-items-center">
  <h2>The answer was...</h2>

  <p class="mt-5">TODO // The answer will appear here</p>

  <h3 class="mt-5">Select the winner</h3>

  <div class="d-flex flex-column" data-controller="update-player-score">
    <% @game.players.each do |player| %>
        <%= link_to "#{player.nickname} • #{player.score || "0 pt"} #{"pts" if player.score}", player_path(player), method: :patch, remote: true, data: { update_player_score_target: 'link', action: 'click->update-player-score#send'}, class: "mt-5 btn btn btn-outline-dark player" %>
    <% end %>
  </div>
</div>
